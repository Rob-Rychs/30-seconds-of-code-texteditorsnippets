<snippet>
	<content><![CDATA[const powerset = arr => arr.reduce((a, v) => a.concat(a.map(r => [v].concat(r))), [[]]);]]></content>
	<description>Returns the powerset of a given array of numbers.  Use `Array.reduce()` combined with `Array.map()` to iterate over elements and combine into an array containing all combinations.</description>
	<tabTrigger>powerset</tabTrigger>
	<scope>source.js</scope>
</snippet>