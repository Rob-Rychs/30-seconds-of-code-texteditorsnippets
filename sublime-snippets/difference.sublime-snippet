<snippet>
	<content><![CDATA[const difference = (a, b) => {
  const s = new Set(b);
  return a.filter(x => !s.has(x));
};]]></content>
	<description> Returns the difference between two arrays.  Create a `Set` from `b`, then use `Array.filter()` on `a` to only keep values not contained in `b`.</description>
	<tabTrigger>difference</tabTrigger>
	<scope>source.js</scope>
</snippet>