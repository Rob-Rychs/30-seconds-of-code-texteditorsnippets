<snippet>
	<content><![CDATA[const ary = (fn, n) => (...args) => fn(...args.slice(0, n));]]></content>
	<description>Creates a function that accepts up to `n` arguments, ignoring any additional arguments.  Call the provided function, `fn`, with up to `n` arguments, using `Array.slice(0,n)` and the spread operator (`...`).</description>
	<tabTrigger>ary</tabTrigger>
	<scope>source.js</scope>
</snippet>