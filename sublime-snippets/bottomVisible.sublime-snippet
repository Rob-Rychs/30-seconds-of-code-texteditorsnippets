<snippet>
	<content><![CDATA[const bottomVisible = () =>
  document.documentElement.clientHeight + window.scrollY >=
  (document.documentElement.scrollHeight || document.documentElement.clientHeight);]]></content>
	<description>Returns `true` if the bottom of the page is visible, `false` otherwise.  Use `scrollY`, `scrollHeight` and `clientHeight` to determine if the bottom of the page is visible.</description>
	<tabTrigger>bottomVisible</tabTrigger>
	<scope>source.js</scope>
</snippet>