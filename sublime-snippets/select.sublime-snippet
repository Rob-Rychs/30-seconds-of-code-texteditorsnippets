<snippet>
	<content><![CDATA[const select = (from, selector) =>
  selector.split('.').reduce((prev, cur) => prev && prev[cur], from);]]></content>
	<description>Retrieve a property that indicated by the selector from an object.  If the property does not exists returns `undefined`.</description>
	<tabTrigger>select</tabTrigger>
	<scope>source.js</scope>
</snippet>