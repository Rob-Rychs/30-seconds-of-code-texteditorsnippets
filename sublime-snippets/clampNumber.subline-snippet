<snippet>
	<content><![CDATA[const clampNumber = (num, a, b) => Math.max(Math.min(num, Math.max(a, b)), Math.min(a, b));]]></content>
	<description>Clamps `num` within the inclusive range specified by the boundary values `a` and `b`.  If `num` falls within the range, return `num`. Otherwise, return the nearest number in the range.</description>
	<tabTrigger>clampNumber</tabTrigger>
	<scope>source.js</scope>
</snippet>