<snippet>
	<content><![CDATA[const lowercaseKeys = obj =>
  Object.keys(obj).reduce((acc, key) => {
    acc[key.toLowerCase()] = obj[key];
    return acc;
  }, {});]]></content>
	<description>Creates a new object from the specified object, where all the keys are in lowercase.  Use `Object.keys()` and `Array.reduce()` to create a new object from the specified object. Convert each key in the original object to lowercase, using `String.toLowerCase()`.</description>
	<tabTrigger>lowercaseKays</tabTrigger>
	<scope>source.js</scope>
</snippet>