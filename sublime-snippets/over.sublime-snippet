<snippet>
	<content><![CDATA[const over = (...fns) => (...args) => fns.map(fn => fn.apply(null, args));]]></content>
	<description>Creates a function that invokes each provided function with the arguments it receives and returns the results.  Use `Array.map()` and `Function.apply()` to apply each function to the given arguments.</description>
	<tabTrigger>over</tabTrigger>
	<scope>source.js</scope>
</snippet>