<snippet>
	<content><![CDATA[const JSONToDate = arr => {
  const dt = new Date(parseInt(arr.toString().substr(6)));
  return `${dt.getDate()}/${dt.getMonth() + 1}/${dt.getFullYear()}`;
};]]></content>
	<description>Converts a JSON object to a date.  Use `Date()`, to convert dates in JSON format to readable format (`dd/mm/yyyy`).</description>
	<tabTrigger>JSONToDate</tabTrigger>
	<scope>source.js</scope>
</snippet>