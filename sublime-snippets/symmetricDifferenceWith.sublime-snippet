<snippet>
	<content><![CDATA[const symmetricDifferenceWith = (arr, val, comp) => [
  ...arr.filter(a => val.findIndex(b => comp(a, b)) === -1),
  ...val.filter(a => arr.findIndex(b => comp(a, b)) === -1)
];]]></content>
	<description> Returns the symmetric difference between two arrays, using a provided function as a comparator.  Use `Array.filter()` and `Array.findIndex()` to find the appropriate values.</description>
	<tabTrigger>symmetricDifferenceWith</tabTrigger>
	<scope>source.js</scope>
</snippet>