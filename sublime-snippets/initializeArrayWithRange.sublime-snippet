<snippet>
	<content><![CDATA[const initializeArrayWithRange = (end, start = 0) =>
  Array.from({ length: end + 1 - start }).map((v, i) => i + start);]]></content>
	<description>Initializes an array containing the numbers in the specified range where `start` and `end` are inclusive.  Use `Array((end + 1) - start)` to create an array of the desired length, `Array.map()` to fill with the desired values in a range. You can omit `start` to use a default value of `0`.</description>
	<tabTrigger>initializeArrayWithRange</tabTrigger>
	<scope>source.js</scope>
</snippet>